English:

Description:
Your goal is to find all bugs in IntervalsAdjacencyDetector class without looking at its internal
implementation.
The test class IntervalsAdjacencyDetectorTest already exists.
Assume all arguments and return values are non-nullable unless annotated with @Nullable annotation.
You should:
1) Open the test class and set up system under test (SUT).
2) Cover the functionality of SUT with tests and identify which requirements aren't satisfied.
3) Read tests output and try to come up with a hypothesis about the production code.
4) Fix the production code.
5) Make sure that all tests pass.

Vietnamese:
Mô tả:
Mục tiêu của bạn là tìm ra mọi lỗi trong lớp IntervalsAdjacencyDetector
mà không cần xem xét đến cách triển khai nội bộ của lớp này.

Lớp kiểm thử IntervalsAdjacencyDetectorTest đã tồn tại.
Giả sử mọi đối số và giá trị trả về đều không thể null trừ khi được chú thích bằng chú thích @Nullable.
Bạn nên:
1) Mở lớp kiểm thử và thiết lập hệ thống đang kiểm thử (SUT).
2) Bao phủ chức năng của SUT bằng các bài kiểm thử và xác định những yêu cầu nào không được đáp ứng.
3) Đọc đầu ra của các bài kiểm thử và cố gắng đưa ra giả thuyết về mã sản xuất.
4) Sửa mã sản xuất.
5) Đảm bảo rằng mọi bài kiểm thử đều vượt qua.